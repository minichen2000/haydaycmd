{
  "name": "haydaycmd",
  "version": "1.1.1",
  "description": "haydaycmd",
  "devDependencies": {
    "bower": "^1.7.9",
    "gulp": "^3.9.1",
    "del": "",
    "gulp-rename": "",
    "http-server": "^0.9.0"
  },
  "scripts": {
	"postinstall": "bower install && gulp clean-bower && gulp",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8082 -c-1"
  }
}
